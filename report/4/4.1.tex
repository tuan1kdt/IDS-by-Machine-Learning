\subsection{Experimental Setup}
\label{sec:exp-setup}

% TODO: describe hardware, software stack, dataset split, random seeds.
%   - Hardware (CPU, RAM, GPU if any)
%   - Software (Python version, scikit-learn, XGBoost, TensorFlow versions)
%   - Dataset split numbers (train / val / test counts per class after stratification)
%   - SMOTE configuration (k=5 neighbours, applied to train only)
%   - Random seeds (split, SMOTE, model init)
