\subsection{Latency and Throughput}
\label{sec:exp-latency}

% TODO: empirical measurement of the inference engine against the SLOs of Section 3.1.
%   - Per-flow latency: p50 and p99 for RF / XGBoost / DNN (single flow, no batching)
%   - Per-flow latency with micro-batching (B=128) — show throughput/latency trade-off
%   - Sustained throughput (flows/s) on the target hardware
%   - Memory footprint (resident MB) of each serialised model + scaler + encoder
%   - Confirm or refute the 10ms p99 budget and 10k flows/s throughput targets
